{
  "name": "dashlab",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix backend ci && npm --prefix web ci",
    "build": "npm --prefix backend run build && npm --prefix web run build",
    "test": "npm --prefix backend test",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
