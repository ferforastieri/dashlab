import{FormEvent,useEffect,useMemo,useState}from'react';import{Activity,CloudSun,HardDrive,LogOut,MemoryStick,MoreVertical,Network,Plus,Search,Server,Settings,Trash2,X}from'lucide-react';import{api,auth,clearAuth,isLogged}from'./api';
type AppItem={id:string;name:string;url:string;description?:string;icon?:string;category?:string;inDock:boolean};type Widget={id:string;type:string;title:string;config:any};type Layout={id:string;kind:string;applicationId?:string;widgetId?:string;order:number};type Dash={id:string;name:string;branding:any;applications:AppItem[];widgets:Widget[];layouts:Layout[]};
const icons:any={nextcloud:'☁️',immich:'🌄',jellyfin:'▶️','shield-checkmark':'🔐','git-branch':'⑂','stats-chart':'📊',shield:'🛡️',server:'▦'};
export function App(){const[logged,setLogged]=useState(isLogged());if(!logged)return<Auth onDone={()=>setLogged(true)}/>;return<Dashboard onLogout={()=>{clearAuth();setLogged(false)}}/>}
function Auth({onDone}:{onDone:()=>void}){const[register,setRegister]=useState(false),[username,setUser]=useState(''),[password,setPass]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{await auth(register?'register':'login',{username,password});onDone()}catch(x:any){setError(x.message)}finally{setBusy(false)}}return<div className="auth-shell"><div className="auth-card"><div className="brand-mark">D</div><h1>DashLab</h1><p>{register?'Crie seu espaço pessoal':'Seu homelab, do seu jeito.'}</p><form onSubmit={submit}><label>Usuário<input value={username} onChange={e=>setUser(e.target.value)} autoCapitalize="none" required minLength={3}/></label><label>Senha<input type="password" value={password} onChange={e=>setPass(e.target.value)} required minLength={8}/></label>{error&&<div className="error">{error}</div>}<button className="primary" disabled={busy}>{busy?'Aguarde…':register?'Criar conta':'Entrar'}</button></form><button className="link" onClick={()=>setRegister(!register)}>{register?'Já tenho uma conta':'Criar uma conta'}</button></div></div>}
function Dashboard({onLogout}:{onLogout:()=>void}){const[dash,setDash]=useState<Dash|null>(null),[metrics,setMetrics]=useState<any>({}),[query,setQuery]=useState(''),[modal,setModal]=useState<'app'|'widget'|'brand'|null>(null),[drag,setDrag]=useState<number|null>(null),[menu,setMenu]=useState<string|null>(null);const load=()=>api('/dashboard?surface=web').then(setDash);useEffect(()=>{load();const tick=()=>api('/metrics/overview').then(setMetrics).catch(()=>{});tick();const i=setInterval(tick,10000);return()=>clearInterval(i)},[]);useEffect(()=>{if(!dash)return;document.title=dash.branding?.name||dash.name;document.documentElement.style.setProperty('--accent',dash.branding?.accent||'#ff7a1a')},[dash]);const ordered=useMemo(()=>{if(!dash)return[];return[...dash.layouts].sort((a,b)=>a.order-b.order)},[dash]);async function drop(at:number){if(drag===null||!dash)return;const next=[...ordered];const[m]=next.splice(drag,1);next.splice(at,0,m);setDrag(null);await api('/layouts/web',{method:'PUT',body:JSON.stringify(next.map((x,i)=>({...x,order:i,x:i%4,y:Math.floor(i/4),w:1,h:1})))});load()}async function remove(kind:string,id:string){if(!confirm('Excluir este item?'))return;await api(`/${kind}/${id}`,{method:'DELETE'});setMenu(null);load()}if(!dash)return<div className="loading">Carregando seu DashLab…</div>;const branding=dash.branding||{};return<div className="desktop" style={{backgroundImage:branding.wallpaper?`linear-gradient(#07101aaa,#07101aaa),url(${branding.wallpaper})`:undefined}}><header><div className="brand"><div className="brand-mark small">{(branding.name||'D')[0]}</div><strong>{branding.name||dash.name}</strong></div><form className="search" onSubmit={e=>{e.preventDefault();if(query)window.open(`https://google.com/search?q=${encodeURIComponent(query)}`,'_blank')}}><Search size={18}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Pesquisar na web"/></form><div className="header-tools"><span><CloudSun size={19}/> Clima</span><Clock/><button className="icon-button" onClick={()=>setModal('brand')} title="Personalizar"><Settings/></button><button className="icon-button" onClick={onLogout} title="Sair"><LogOut/></button></div></header><main><section className="app-grid">{ordered.map((layout,index)=>{const app=layout.kind==='APPLICATION'?dash.applications.find(a=>a.id===layout.applicationId):null;const widget=layout.kind==='WIDGET'?dash.widgets.find(w=>w.id===layout.widgetId):null;if(!app&&!widget)return null;return<div key={layout.id} draggable onDragStart={()=>setDrag(index)} onDragOver={e=>e.preventDefault()} onDrop={()=>drop(index)}>{app?<div className="app-wrap"><a className="app-icon" href={app.url} target="_blank" rel="noreferrer"><span>{app.icon?.startsWith('http')?<img src={app.icon}/>:icons[app.icon||'']||'◉'}</span></a><b>{app.name}</b><button className="item-menu" onClick={()=>setMenu(menu===app.id?null:app.id)}><MoreVertical/></button>{menu===app.id&&<div className="context"><button onClick={()=>remove('applications',app.id)}><Trash2/> Excluir</button></div>}</div>:<WidgetCard widget={widget!} metrics={metrics} onDelete={()=>remove('widgets',widget!.id)}/>}</div>})}</section></main><div className="dock">{dash.applications.filter(a=>a.inDock).map(a=><a href={a.url} target="_blank" key={a.id}><span>{icons[a.icon||'']||'◉'}</span></a>)}</div><button className="add" onClick={()=>setModal('app')}><Plus/></button>{modal&&<Editor type={modal} dash={dash} close={()=>setModal(null)} done={()=>{setModal(null);load()}}/>}</div>}
function Clock(){const[now,setNow]=useState(new Date());useEffect(()=>{const i=setInterval(()=>setNow(new Date()),1000);return()=>clearInterval(i)},[]);return<span>{now.toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}</span>}
function WidgetCard({widget,metrics,onDelete}:{widget:Widget;metrics:any;onDelete:()=>void}){const map:any={SYSTEM:[Activity,'CPU',metrics.cpu,'Memória',metrics.memory],STORAGE:[HardDrive,'Disco',metrics.disk],NETWORK:[Network,'Download',metrics.download?`${(metrics.download/1e6).toFixed(1)} MB/s`:'—','Upload',metrics.upload?`${(metrics.upload/1e6).toFixed(1)} MB/s`:'—'],CLOCK:[Server,'Agora',new Date().toLocaleTimeString('pt-BR')],WEATHER:[CloudSun,'Clima','—'],SEARCH:[Search,'Pesquisa','Google'],PROMQL:[Activity,'Métrica','…']};const data=map[widget.type]||[MemoryStick,widget.title,'—'];const Icon=data[0];return<div className="widget"><button className="widget-delete" onClick={onDelete}><X/></button><div className="widget-title"><Icon/>{widget.title}</div><div className="widget-values">{data.slice(1).map((x:any,i:number)=><span key={i} className={typeof x==='number'?'metric':''}>{typeof x==='number'?`${x.toFixed(0)}%`:x}</span>)}</div></div>}
function Editor({type,dash,close,done}:{type:string;dash:Dash;close:()=>void;done:()=>void}){const[mode,setMode]=useState(type),[form,setForm]=useState<any>({name:'',url:'https://',icon:'',inDock:false,title:'',type:'SYSTEM',config:{},...dash.branding});async function save(e:FormEvent){e.preventDefault();if(mode==='app')await api('/applications',{method:'POST',body:JSON.stringify(form)});else if(mode==='widget')await api('/widgets',{method:'POST',body:JSON.stringify({...form,config:form.type==='PROMQL'?{query:form.query}:form.config})});else await api('/branding',{method:'PUT',body:JSON.stringify(form)});done()}return<div className="overlay" onMouseDown={e=>e.target===e.currentTarget&&close()}><form className="modal" onSubmit={save}><button type="button" className="close" onClick={close}><X/></button>{type!=='brand'&&<div className="tabs"><button type="button" className={mode==='app'?'active':''} onClick={()=>setMode('app')}>Aplicativo</button><button type="button" className={mode==='widget'?'active':''} onClick={()=>setMode('widget')}>Widget</button></div>}<h2>{mode==='brand'?'Personalizar meu DashLab':mode==='app'?'Novo aplicativo':'Novo widget'}</h2>{mode==='app'&&<><label>Nome<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Link<input required value={form.url} onChange={e=>setForm({...form,url:e.target.value})}/></label><label>Ícone (URL ou nome)<input value={form.icon} onChange={e=>setForm({...form,icon:e.target.value})}/></label><label className="check"><input type="checkbox" checked={form.inDock} onChange={e=>setForm({...form,inDock:e.target.checked})}/> Colocar no dock</label></>}{mode==='widget'&&<><label>Título<input required value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></label><label>Tipo<select value={form.type} onChange={e=>setForm({...form,type:e.target.value})}>{['SYSTEM','STORAGE','NETWORK','CLOCK','WEATHER','SEARCH','STATUS','PROMQL'].map(x=><option key={x}>{x}</option>)}</select></label>{form.type==='PROMQL'&&<label>Consulta PromQL<textarea value={form.query||''} onChange={e=>setForm({...form,query:e.target.value})}/></label>}</>}{mode==='brand'&&<><label>Nome<input value={form.name||''} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Wallpaper (URL)<input value={form.wallpaper||''} onChange={e=>setForm({...form,wallpaper:e.target.value})}/></label><label>Cor de destaque<input type="color" value={form.accent||'#ff7a1a'} onChange={e=>setForm({...form,accent:e.target.value})}/></label></>}<button className="primary">Salvar</button></form></div>}
