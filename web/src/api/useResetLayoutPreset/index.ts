import {useMutation,useQueryClient} from '@tanstack/react-query';import {client} from '../client';import {keys} from '../keys';export const useResetLayoutPreset=()=>{const q=useQueryClient();return useMutation({mutationFn:async(preset:string)=>(await client.post(`/layout-presets/${preset}/reset`,{surface:'WEB'})).data,onSuccess:()=>q.invalidateQueries({queryKey:keys.dashboard})})};
