name: dashlab

services:
  api:
    build: { context: ., dockerfile: Dockerfile.api }
    container_name: dashlab-api
    restart: unless-stopped
    environment:
      DATABASE_PATH: /data/dashlab.db
      PROMETHEUS_URL: ${PROMETHEUS_URL:-}
      PROMETHEUS_TARGET_LABELS: ${PROMETHEUS_TARGET_LABELS:-}
      PROMETHEUS_NETWORK_LABELS: ${PROMETHEUS_NETWORK_LABELS:-device!="lo"}
      PROMETHEUS_DISK_LABELS: ${PROMETHEUS_DISK_LABELS:-device=~"nvme[0-9]+n[0-9]+|sd[a-z]+"}
      PORT: 3001
    volumes: [dashlab_data:/data]
    healthcheck:
      {
        test: ['CMD', 'wget', '-qO-', 'http://127.0.0.1:3001/api/health'],
        interval: 20s,
        timeout: 5s,
        retries: 6,
      }
  web:
    build: { context: ., dockerfile: Dockerfile.web }
    container_name: home-web
    restart: unless-stopped
    ports: ['${WEB_PORT:-3000}:80']
    depends_on: { api: { condition: service_healthy } }
volumes:
  dashlab_data:
    name: dashlab_data
